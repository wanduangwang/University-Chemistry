# MyST project build dependencies for UNIVERSITY-CHEMISTRY.
#
# NOTE: The site itself is built with `mystmd` (Node.js / npm), installed in CI via
# `npm install -g mystmd`. The Python packages below mirror the reference project
# (SolidStatePhysics / General-Chemistry) and are kept for parity / optional
# executed-notebook or plotting support.
pymupdf==1.28.0
contourpy==1.3.1
cycler==0.12.1
fonttools==4.61.0
kiwisolver==1.4.8
matplotlib==3.10.0
numpy==2.2.2
packaging==24.2
pandas==2.2.3
pillow==12.1.1
pyparsing==3.2.1
python-dateutil==2.9.0.post0
pytz==2025.1
scipy==1.15.3
seaborn==0.13.2
six==1.17.0
tzdata==2025.1
plotly
anywidget
ipywidgets
